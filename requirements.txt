# Huginn voice runtime (install into ~/speech/.venv):
#   uv venv .venv --python 3.12
#   uv pip install --python .venv/bin/python -r requirements.txt
# Kokoro-82M weights auto-download to ~/.cache/huggingface on first use.
mlx-audio
misaki[en]
soundfile
numpy
